# Dependencias y artefactos locales
node_modules
.next
out

# Entorno y datos locales (Railway/Docker inyectan DATABASE_URL)
.env
.env.*
!.env.example

# Git y IDE
.git
.gitignore
.cursor
.cursorrules
ai-team

# SQLite legado / volúmenes
dev.db
dev.db-*

# Docs y metadatos no necesarios en la imagen
*.md
docs
ai-team

# Tests / cobertura (si se añaden)
coverage
.nyc_output

# Prisma: no excluir prisma/schema.prisma — el Dockerfile copia prisma/ antes de npm ci y postinstall ejecuta prisma generate.
prisma/seed.js
prisma/sql
