# Copia a `.env` y completa los valores (no subas `.env` al repo).
DATABASE_URL="postgresql://USER:PASSWORD@HOST:PORT/DATABASE"

# Demo portafolio: `true` desactiva reservas y el formulario (solo visualización).
NEXT_PUBLIC_PORTFOLIO_DEMO="true"
